#!/usr/bin/env bash
# deploy-devnet.sh — upload the DAR to Canton DevNet and verify reachability.
# Reads DevNet endpoints + credentials from the environment; no hardcoded hosts.
# // VERIFY: DevNet onboarding/faucet flow against docs.global.canton.network.
set -euo pipefail
echo "deploy-devnet.sh — not yet implemented."
