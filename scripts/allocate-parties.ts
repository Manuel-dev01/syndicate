/**
 * allocate-parties.ts — allocate the demo parties on the target ledger and print their ids.
 * Implemented in Phase 3. Reads the ledger endpoint from environment variables; works against
 * LocalNet and Canton DevNet without code changes.
 */
export {};
