/**
 * init-ledger.ts — seed a demo facility + three lender positions for the demo.
 * Implemented in Phase 3. Creates the shared Facility (borrower + agent bank) and one
 * LenderPosition per lender via the Ledger API, preserving the privacy partition.
 */
export {};
