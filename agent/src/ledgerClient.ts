/**
 * ledgerClient.ts — authorized Daml choice execution via the Ledger API.
 * Exercises ONLY choices the agent's party is authorized for;
 * uses the agent's own party-scoped token (never the frontend's). All endpoints come from
 * environment variables — no hardcoded localhost.
 */
export {};
